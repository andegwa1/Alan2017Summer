<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.al.alan2017summer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                /
            </intent-filter>
        </activity>

        <!-- <activity android:name=".TestActivity"> -->


        <!-- </activity> -->

        <activity android:name=".ViewPagerActivity" />

        <activity android:name=".ActivityA"
            android:launchMode="standard" />
        <activity android:name=".ScaleTypeActivity" />

        <activity android:name=".ActivityB"
            android:launchMode="singleTop"/>

        <activity android:name=".ActivityC"
            android:launchMode="singleTask"/>

        <activity android:name=".ActivityD"
            android:launchMode="singleInstance"/>

        <activity android:name=".NinePatchActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".fragment.AdvanceActivity" />
        <activity android:name=".AdvanceListViewActivity"></activity>
    </application>

</manifest>